sensor:
  type: DHT22
  pin: 4
  interval: 10  # seconds

mqtt:
  broker: mqtt://localhost:1883
  topic: environmental_data
  client_id: edge_node_client

logging:
  level: INFO
  file: /var/log/edge_node.log

data:
  retention_period: 30  # days
  storage_path: /data/environmental_data.json